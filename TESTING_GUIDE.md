# RemoteCheckin v2.0 - 测试指南

## 🎯 快速开始

### 前置要求检查

#### 1. Java环境
```bash
# 检查Java版本（需要JDK 8+）
java -version
```

您的系统已安装:
- ✅ JDK 1.8.0_202

#### 2. Android Studio（推荐）
由于没有检测到Android SDK，推荐使用Android Studio:

**下载地址**: https://developer.android.com/studio

### 测试方案

## 方案A: 使用Android Studio（推荐）

### 步骤1: 安装Android Studio
1. 下载并安装Android Studio
2. 启动Android Studio
3. 安装Android SDK（会提示安装）

### 步骤2: 打开项目
```
1. 启动 Android Studio
2. 选择 "Open"
3. 浏览到: D:\github\remote_dingding
4. 点击 "OK"
5. 等待Gradle同步完成（首次可能需要几分钟）
```

### 步骤3: 配置Google Maps API
1. 访问 https://console.cloud.google.com
2. 创建项目并启用Maps SDK for Android
3. 创建API密钥
4. 在 `app/src/main/AndroidManifest.xml` 中添加:
```xml
<meta-data
    android:name="com.google.android.geo.API_KEY"
    android:value="YOUR_API_KEY_HERE" />
```

### 步骤4: 运行测试
```
1. 连接Android设备（USB调试）或启动模拟器
2. 点击绿色运行按钮 ▶️
3. 选择目标设备
4. 等待安装完成
```

## 方案B: 命令行构建

### 步骤1: 安装Android SDK命令行工具

1. **下载Command Line Tools**
   - 访问: https://developer.android.com/studio#command-tools
   - 下载 "Command line tools only"

2. **解压并配置**
```bash
# 创建目录
mkdir C:\Android\sdk
cd C:\Android\sdk

# 解压下载的文件到 cmdline-tools\latest

# 设置环境变量
set ANDROID_HOME=C:\Android\sdk
set PATH=%PATH%;%ANDROID_HOME%\cmdline-tools\latest\bin;%ANDROID_HOME%\platform-tools
```

3. **安装必要组件**
```bash
sdkmanager "platform-tools" "platforms;android-34" "build-tools;34.0.0"
```

### 步骤2: 下载Gradle Wrapper JAR
```bash
# 在项目根目录执行
cd D:\github\remote_dingding

# 下载Gradle Wrapper
powershell -Command "Invoke-WebRequest -Uri 'https://services.gradle.org/distributions/gradle-8.2-bin.zip' -OutFile 'gradle-8.2-bin.zip'"

# 解压
powershell -Command "Expand-Archive -Path 'gradle-8.2-bin.zip' -DestinationPath '.'"
```

### 步骤3: 构建项目
```bash
cd D:\github\remote_dingding
gradlew.bat assembleDebug
```

## 方案C: 使用在线构建服务

### 1. GitHub Actions（推荐）
将项目推送到GitHub，使用GitHub Actions自动构建：

**创建 `.github/workflows/build.yml`**:
```yaml
name: Build APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3

    - name: Set up JDK 11
      uses: actions/setup-java@v3
      with:
        java-version: '11'
        distribution: 'temurin'
        cache: gradle

    - name: Grant execute permission for gradlew
      run: chmod +x gradlew

    - name: Build with Gradle
      run: ./gradlew assembleDebug

    - name: Upload APK
      uses: actions/upload-artifact@v3
      with:
        name: app-debug
        path: app/build/outputs/apk/debug/app-debug.apk
```

### 2. Replit（在线IDE）
1. 访问 https://replit.com
2. 创建新的Android项目
3. 上传代码
4. 在线构建和运行

## 📱 功能测试清单

### 基础功能测试

#### ✅ 1. 应用启动
- [ ] 应用成功安装
- [ ] 启动无崩溃
- [ ] 主界面正常显示
- [ ] 权限请求正常弹出

#### ✅ 2. 位置管理
- [ ] 点击"添加位置"打开地图
- [ ] 地图正常加载
- [ ] 点击地图选择位置
- [ ] 输入位置名称
- [ ] 成功保存位置
- [ ] 位置列表显示正确

#### ✅ 3. 位置模拟
- [ ] 选择一个已保存的位置
- [ ] 点击"模拟位置"按钮
- [ ] 通知栏显示"位置模拟运行中"
- [ ] 打开Google Maps验证位置
- [ ] 位置显示为模拟的位置

#### ✅ 4. 轨迹模拟
- [ ] 点击"轨迹模拟"按钮
- [ ] 地图打开
- [ ] 点击"规划轨迹"
- [ ] 在地图上添加多个点
- [ ] 点击"完成轨迹"
- [ ] 轨迹开始模拟
- [ ] 位置按轨迹移动

#### ✅ 5. 定时打卡
- [ ] 选择一个位置
- [ ] 点击"定时打卡"按钮
- [ ] 时间选择器弹出
- [ ] 选择打卡时间
- [ ] 确认后显示"已安排打卡"
- [ ] 到达时间自动执行

#### ✅ 6. 打卡记录
- [ ] 点击"查看打卡记录"按钮
- [ ] 显示历史记录列表
- [ ] 记录信息完整（时间、位置、状态）

### 高级功能测试（需要root或Xposed）

#### ✅ 7. Hook框架
**前提**: 设备已root并安装Xposed框架
- [ ] 在Xposed Installer中启用本应用模块
- [ ] 重启设备
- [ ] 启动Hook服务
- [ ] 打开目标应用（如抖音）
- [ ] 验证位置被Hook

#### ✅ 8. 设备伪装
- [ ] 设置设备模板（如小米Mi 10）
- [ ] 使用Hook模式
- [ ] 打开应用查看设备信息
- [ ] 验证设备信息已修改

#### ✅ 9. 应用白名单
- [ ] 查看预设应用列表
- [ ] 添加应用到白名单
- [ ] 设置应用独立位置
- [ ] 验证不同应用显示不同位置

#### ✅ 10. Root检测绕过
- [ ] 启用Xposed模块
- [ ] 打开需要检测root的应用
- [ ] 验证root检测被绕过

### 性能测试

#### ✅ 11. 内存占用
```
测试方法:
1. 打开应用
2. 使用adb shell dumpsys meminfo com.example.remotecheckin
3. 查看内存占用

预期: < 150MB
```

#### ✅ 12. CPU占用
```
测试方法:
1. 运行位置模拟
2. 使用adb shell top | grep com.example.remotecheckin
3. 查看CPU占用

预期: < 10%
```

#### ✅ 13. 电池消耗
```
测试方法:
1. 充满电
2. 运行位置模拟1小时
3. 查看电池使用统计

预期: < 5%/小时
```

### 兼容性测试

#### ✅ 14. Android版本
- [ ] Android 7.0 (API 24)
- [ ] Android 8.0 (API 26)
- [ ] Android 9 (API 28)
- [ ] Android 10 (API 29)
- [ ] Android 11 (API 30)
- [ ] Android 12 (API 31)
- [ ] Android 13 (API 33)
- [ ] Android 14 (API 34)

#### ✅ 15. 设备品牌
- [ ] 小米
- [ ] 华为
- [ ] 三星
- [ ] OPPO
- [ ] vivo
- [ ] 一加

## 🐛 常见问题解决

### 问题1: 编译错误 "找不到R.java"
**原因**: 资源文件缺失
**解决**:
```bash
# 清理并重建
gradlew.bat clean
gradlew.bat assembleDebug
```

### 问题2: Google Maps无法显示
**原因**: API密钥未配置或无效
**解决**:
1. 检查API密钥是否正确配置
2. 确认Maps SDK已启用
3. 检查API密钥限制
4. 验证SHA-1指纹

### 问题3: 模拟位置不生效
**原因**: 未在开发者选项中选择应用
**解决**:
```
设置 → 系统 → 开发者选项 → 选择模拟位置信息应用
→ 选择"远程打卡助手"
```

### 问题4: 权限被拒绝
**原因**: 用户拒绝了权限
**解决**:
```
设置 → 应用 → 远程打卡助手 → 权限
→ 授予所有权限
```

### 问题5: Xposed模块不生效
**原因**: 模块未启用或未重启
**解决**:
1. 在Xposed Installer中启用模块
2. 重启设备
3. 检查模块日志

## 📊 测试报告模板

```markdown
# 测试报告

**测试日期**: YYYY-MM-DD
**测试人员**: [姓名]
**设备型号**: [设备]
**Android版本**: [版本]

### 测试结果

#### 功能测试
- 应用启动: ✅/❌
- 位置管理: ✅/❌
- 位置模拟: ✅/❌
- 轨迹模拟: ✅/❌
- 定时打卡: ✅/❌
- 打卡记录: ✅/❌
- Hook框架: ✅/❌/N/A
- 设备伪装: ✅/❌/N/A
- 应用白名单: ✅/❌/N/A

#### 性能测试
- 内存占用: ___ MB
- CPU占用: ___ %
- 电池消耗: ___ %/小时

#### 发现的问题
1. [问题描述]
2. [问题描述]

#### 建议和改进
1. [建议]
2. [建议]
```

## 🎯 下一步

测试完成后，您可以：

### 1. 反馈问题
- 记录发现的问题
- 提供详细日志
- 描述复现步骤

### 2. 优化改进
- 根据测试结果优化
- 修复发现的问题
- 添加新功能

### 3. 发布应用
- 生成Release签名APK
- 准备应用上架
- 编写用户文档

## 📞 获取帮助

- 查看日志: `adb logcat | grep RemoteCheckin`
- 查看数据库: `adb shell dumpsys dbinfo`
- 查看位置: `adb shell dumpsys location`

---

*准备好了吗？开始测试吧！* 🚀
